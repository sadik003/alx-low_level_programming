#include "main.h"
#include <stdio.h>

/**
 * main - the function prints the number of arguments passed
 * @argc: argument counter
 * @argv: argument vector
 * Return: 0 on succss
 */

int main(int argc, char **argv __attribute__((unused)))
{
	printf("%d\n", argc);

	return (0);
}
