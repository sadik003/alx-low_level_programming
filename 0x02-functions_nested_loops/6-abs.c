#include "main.h"

/**
 * _abs - a function that compute the absolute vallue of an integer
 *
 * @n: the integer
 *
 * Return: 0
 *
 */
	int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
