#include "main.h"

/**
 * _islower - the function check if character is lowercase
 * @c: character to be checked
 * Return: 1 for success and 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');

}
