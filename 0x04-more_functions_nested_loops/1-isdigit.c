#include "main.h"

/**
 * _isdigit - the function checks for digit between 0 and 9
 * @c: the digit to be checked
 * Return: 1 if success and 0 if failed
 */

int _isdigit(int c)
{
	return (c >= 0 && c <= 9);
}
