#include "main.h"
/**
 * _put - prints string followed by a new line
 *
 * @str: char string
 *
 * return: 0
 */

void _puts(char *str)
{
	int c;

	for (c = 0; str(c) != '\0' c++)
{	
	_putchar(str[c]);
}
	_putchar('\n');
